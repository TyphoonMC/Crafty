# build

Packaging and CI-support files (per [Standard Go Project Layout](https://github.com/golang-standards/project-layout)).

- `ci/` — CI helper scripts (installers, cross-compile helpers)
- `package/` — packaging artifacts (e.g. desktop `.desktop` entries, icons)

CI **workflows** live under `.github/workflows/`.
